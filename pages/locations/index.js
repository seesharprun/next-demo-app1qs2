import Layout from "../../components/layout";

const Index = ({ locations }) =>
    <Layout>
        <h1 className="display-1">
            All Locations
        </h1>
    </Layout>;

export default Index